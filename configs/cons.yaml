data:
 target: data.data_interface.DInterface
 params:
  train_dataset_dir: /home/work/daixingshuo/RSISR/dataset/data/pngimage_train_HR
  val_dataset_dir: /home/work/daixingshuo/RSISR/dataset/data/pngimage2_valid_HR
  test_dataset_dir: /home/work/daixingshuo/RSISR/dataset/data
  crop_size: 256
  upscale_factor: 4
  batch_size: 5
  wrap: bool
model:
 model_name: upscale_model
 color_range: 255
 lr: 1.0e-04
 loss: l1
 weight_decay: 1e-5
 lr_scheduler: 'step'
 lr_decay_steps: 20
 lr_decay_rate: 0.5
 lr_decay_min_lr: 1e-5
 checkpoint_loaddir: /share/program/dxs/RSISR/checkpoint
 checkpoint_version: 0
 load_v_num: 1
 image_size: 256
 model_config:
  upsample:
   in_channels: 3
   out_channels: 3
   attn: 'ch_att'
   upscale_factor: 4
  encoder:
   model_path: "/home/work/daixingshuo/RSISR/pretrain_weights"
  ddpm: 
  decoder:
   model_path: "/home/work/daixingshuo/RSISR/pretrain_weights"
trainer:
 max_epochs: 500
 gpus: 3
 precision: 16
 accelerator: "ddp_spawn"
callbacks:
 lr_scheduler: step
other_params:
 trainer_stage: 'train'
 seed: 23